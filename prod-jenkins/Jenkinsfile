pipeline {
  agent any 
  stages {
    stage('Deploy') { 
      steps {
        sh '''
          ssh -o StrictHostKeyChecking=no -tt sifiso@localhost
          cd ~/node-server
          git pull
          yarn
          yarn restart
          exit
        '''
      }
    }
  }
}
